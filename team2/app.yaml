apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  namespace: team2
spec:
  replicas: 4
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: team2
spec:
  selector:
    app: myapp
  ports:
    - port: 80
      targetPort: 80
